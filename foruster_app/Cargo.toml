# SPDX-License-Identifier: GPL-3.0-or-later
[package]
name = "foruster"
version = "0.0.1"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
slint = "1.9.2"

foruster_storage = { path = "../crates/foruster_storage" }

[build-dependencies]
slint-build = "1.9.2"
