// SPDX-License-Identifier: GPL-3.0-or-later
import { DisksScreenComponent } from "../components/index.slint";

export component MainView {
    DisksScreenComponent {
        width: parent.width;
        height: parent.height;
    }
}
