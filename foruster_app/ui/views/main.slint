// SPDX-License-Identifier: GPL-3.0-or-later
import { DiskListView } from "./disk_list_view.slint";

export component MainView {
    DiskListView {
        width: parent.width;
        height: parent.height;
    }
}
